idf_component_register(
    SRCS
        "main.c"
        "app_config.c"
        "hostname_validation.c"
        "gps_uart.c"
        "nmea_timing.c"
        "ntp_path_diagnostics.c"
        "ntp_server.c"
        "ntp_time.c"
        "pps_diagnostics.c"
        "pps.c"
        "timebase.c"
        "ethernet.c"
        "mdns_service.c"
        "web_assets.c"
        "web_server.c"
    INCLUDE_DIRS
        "."
    REQUIRES
        esp_app_format
        esp_driver_gpio
        esp_driver_mcpwm
        esp_driver_spi
        esp_driver_uart
        esp_eth
        esp_event
        esp_http_server
        esp_netif
        esp_timer
        lwip
        mdns
        nvs_flash
        spi_flash
)

# Decorate the W5500 GPIO handler without modifying the managed component.
target_link_options(
    ${COMPONENT_LIB}
    INTERFACE
    "-Wl,--wrap=gpio_isr_handler_add"
)
